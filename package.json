{
  "name": "monorepo-demo-2-root",
  "private": true,
  "workspaces": [
    "webapp",
    "packages/*"
  ],
  "scripts": {
    "build:ui": "cd packages/ui && tsc",
    "dev": "npm run build:ui && cd webapp && npm run dev"
  }
}